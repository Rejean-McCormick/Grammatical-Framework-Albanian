# DRAFT-MAX preserved development line

These directories are intentionally outside `AlbanianSQI/GF/lib/src` and therefore are not active grammar modules.

- `checkpoint1/`: exact checkpoint-1 material.
- `checkpoint2/`: exact checkpoint-2 material, including the richer typed-clitic/voice implementation and migration notes.

Do not copy the checkpoint-2 payload wholesale into the active tree. Reintegrate feature groups incrementally after propagating their type contracts through the complete snapshot.
