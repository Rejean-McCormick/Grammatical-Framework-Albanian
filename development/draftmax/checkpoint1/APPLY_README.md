# Apply — Albanian DRAFT-MAX checkpoint 1

This is a development checkpoint, not a release overlay.
Apply it only on top of the Albanian linguistic-completion state from 2026-09-24.
It intentionally may require propagation fixes before GF compiles again.

Copy `payload/` over the repository root, preserving paths. Keep a backup or use a
separate branch/worktree because this checkpoint changes core category contracts.
