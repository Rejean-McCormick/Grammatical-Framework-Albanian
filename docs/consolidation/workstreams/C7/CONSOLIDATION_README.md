# Albanian GF — C07 clause-syntax consolidated candidate

This tree preserves the supplied Albanian snapshot and layers the Conversation 7 clause-syntax finalization candidate on top of it.

The active C07 changes are documented under `docs/clause/`. Historical consolidation, Dict/MorphoDict, DRAFT-MAX and earlier repair documents remain preserved as provenance; they do not override the C07 patch record for clause-syntax claims.

No DRAFT-MAX overlay is activated wholesale. No shared `CatSqi`/`ResSqi` representation migration is part of C07.

Current C07 validation boundary: static/contract checks pass locally; fresh GF 3.12 compiler and executable scenario certification must be produced in the maintainer environment.
