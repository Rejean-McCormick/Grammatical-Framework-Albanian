# C9 release-surface smoke

This test grammar composes the normal `LangSqi` surface with `SymbolSqi` and provides ready-to-run GF cases for:

- `DocumentationSqi`: N, A, V, Prep and the newly completed Cl inflection document;
- `MarkupSqi`: NP, CN, AP and S rich-category wrapping;
- `SymbolSqi`: symbolic NP and symbolic sentence realization.

It is additive and does not alter the protected `validation/scenarios` set.  Status in the candidate: **not executed** because GF 3.12 is unavailable in the current environment.
