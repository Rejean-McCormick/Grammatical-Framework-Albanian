# Reviewed gold outputs

This directory contains only explicitly promoted, hash-bound reviewed scenario outputs. Normal validation does not create or update golds.

Gold promotion is performed only through the C10 `promote-golds` command and must record reviewer identity, decision reference, metadata and diff under `validation/certification/reviews/gold_promotions/`.
