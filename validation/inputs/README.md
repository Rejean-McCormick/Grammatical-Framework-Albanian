# Validation input modules

These GF input modules support the registered Albanian validation scenarios. They are part of the C10 working-tree lock.

Changes require regeneration of the lock and invalidate stale execution/review evidence where hashes no longer match.
